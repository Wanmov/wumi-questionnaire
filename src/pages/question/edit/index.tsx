import { useGetQuestionInfo } from '../../../hooks/useGetQuestionInfo';

const Edit: React.FC = () => {
  const { loading, data } = useGetQuestionInfo();
  return <></>;
};

export default Edit;
